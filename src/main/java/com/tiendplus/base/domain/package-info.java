/**
 * Este paquete contiene clases de dominio reutilizables.
 */
@NullMarked // Indica que todas las clases y métodos dentro de este paquete tienen valores no nulos por defecto, a menos que se indique lo contrario.
@NonNullApi // Indica que todos los parámetros y valores de retorno de las clases dentro de este paquete no pueden ser nulos, a menos que se marque explícitamente como nulo.
package com.tiendplus.base.domain;

import org.jspecify.annotations.NullMarked; // Anotación que marca todas las clases y métodos en este paquete como no nulos por defecto.
import org.springframework.lang.NonNullApi;  // Anotación de Spring que asegura que los parámetros y valores de retorno no sean nulos por defecto dentro del paquete.
